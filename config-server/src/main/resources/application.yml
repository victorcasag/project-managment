server:
  port: 8888

spring:
  cloud:
    config:
      server:
        bootstrap: true
  profiles:
    active: native

spring:
  profiles: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./config-repo

management:
  endpoints:
    web:
      exposure:
        include: "*"
